import React from 'react';
import './Navbar.scss';

const Navbar = () => {
  return (
    <div>
      <nav>
        <div>
          <img src="{images.logo}" alt="" />
        </div>
      </nav>
    </div>
  );
};

export default Navbar;
